import { useEffect, useState } from "react";
import useGeolocation from "../../hooks/useGeolocation";

export const useCurrentLocation = () => {   /// fehlerhaft. useGeolocation reicht aus! Auf useGeolocation konzentrieren. 

}